version: 2

exposures:
  - name: weekly_metrics
    label: saisri by the week
    type: dashboard
    maturity: high
    description: "For more exposure about project details"

    depends_on:
      - ref('final_business')
      - ref('order_merge')
      - ref('order_macro_data')
      - ref('business_table')
      - source('datafeed_shared_schema', 'sraw_customerdata')
      - source('datafeed_shared_schema', 'customers')
      - source('datafeed_shared_schema', 'sorders')

      owner: 
      name: shaikhidayathulla
      email: shaikhidayath265@gmail.com
